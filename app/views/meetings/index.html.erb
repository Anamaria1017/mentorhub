<%= month_calendar(events: @meetings) do |date, meetings| %>
  <%= date %>

  <% meetings.each do |meeting| %>
    <div>
      <%= link_to meeting.name, meeting %>
      <%= meeting.start_time %>
      <%= meeting.end_time %>
      <%= meeting.location %>
    </div>
  <% end %>
<% end %>
